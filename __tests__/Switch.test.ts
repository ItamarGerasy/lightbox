import {Switch, SwitchesMap} from "../src/framework/Switch"
const mockDimensions = {
    width: 1.1,
    height: 2,
    depth: 3
}

// describe("Switch", () => {
//     it("should create a switch object", () => {
//         let newSwitch = new Switch("s1", "lights", "blabla", "1X16", mockDimensions, "PC")
//         expect(typeof newSwitch).toEqual(Switch)
//     })
// })